// 7 primitive datatypes : 
// nn bb ss u :

let Null = null;
let number = 2;

let bOolean = true;
let biGInt = BigInt(234) + BigInt(1);

let StRing = "Tanoy";
let symBol = Symbol("Hey Nate !");

let UndefIned = undefined; 

console.log(Null, number, bOolean, biGInt, StRing,symBol,UndefIned);

// Non - primitive data types : 

const items = {
    "Tanoy": true,
    "Jubin": false,
    "LbishBi": 11,
    "Ola": undefined
}
console.log(items.Ola);