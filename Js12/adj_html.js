// first.insertAdjacentHTML("beforebegin",`<div class="test">nf</div>`);
// first.insertAdjacentHTML("beforeend",`<div class="test">nf</div>`)
// first.insertAdjacentHTML("afterbegin",`<div class="test">nf</div>`)
// first.insertAdjacentHTML("afterend",`<div class="test">nf</div>`)
first.remove(); // removes the element with id first.
